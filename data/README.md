# 本地数据目录

`data/raw/` 保存按内容寻址的原始快照，`data/curated/` 保存经过 point-in-time
过滤和质量检查的研究数据集。两者默认不进入 Git，因为真实行情可能受供应商
条款约束，也可能体积很大。

不要手工修改已有目录。源数据、许可清单、研究范围或 as-of 任一变化，都会
生成新的 `snapshot_id` 或 `dataset_id`。回测产物会复制对应的数据集 manifest，
因此旧结果不会悄悄指向后来修订的数据。

若需要长期保存，应把整个目录同步到只追加、带版本控制的私有对象存储，而不
只是保留最新 CSV。
