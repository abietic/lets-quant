# 贡献指南

## 边界

贡献必须保持项目的 research-only、local-first 和 manual-approval 边界。不要提交：

- 自动下单、绕过人工确认或默认启用真实执行的能力。
- API key、券商凭证、真实账户导出、真实持仓或个人身份信息。
- 未确认再分发权的行情数据、研报或第三方专有策略。
- 把回测、Paper 或 AI 生成结果描述为收益保证的文案。

示例数据必须是可公开再分发的合成 fixture；如使用真实证券代码，需明确说明价格、
停牌和公司行动记录是否为合成数据。

## 提交流程

1. 从最新 `main` 创建短生命周期分支。
2. 运行 `make ci`；可选引擎变更还需运行对应的 `make vectorbt-test` 或
   `make rqalpha-test`。
3. 保持变更范围单一，并为行为变化添加失败边界测试。
4. pull request 说明研究假设、风险边界、验证证据和未验证部分。

依赖升级不能仅凭自动化来源合并，仍需审阅变更日志、许可证和本地测试结果。
